apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus-lb
  name: prometheus-lb
  namespace: monitoring
spec:
  ports:
  - name: 9090-9090
    port: 9090
    protocol: TCP
    targetPort: 9090
  selector:
    app: prometheus-lb
  type: LoadBalancer
